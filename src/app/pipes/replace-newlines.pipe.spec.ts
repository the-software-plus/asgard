import { ReplaceNewlinesPipe } from './replace-newlines.pipe';

describe('ReplaceNewlinesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceNewlinesPipe();
    expect(pipe).toBeTruthy();
  });
});
